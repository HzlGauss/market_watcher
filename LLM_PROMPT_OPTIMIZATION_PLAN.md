# LLM 提示词优化方案

## 概述

本文档从提示词工程和投资专业两个视角，分析当前系统中所有 LLM 提示词的改进空间，并给出可落地的修改方案。

涉及的代码文件：
- `app/llm_client.py`（SYSTEM_PROMPTS 字典）
- `app/ai_analyzer.py`（`_build_prompt` 实时盯盘分析）
- `app/reporter.py`（Morning Brief / Midday Review / Evening Review 三个报告）
- `app/fund_analyzer.py`（基金深度分析报告）

---

## 一、System Prompt 重写（llm_client.py）

### 当前问题

5 个角色设定全部只有一句话，缺少身份锚点和分析框架约束。

### 修改方案

替换 `SYSTEM_PROMPTS` 字典如下：

```python
SYSTEM_PROMPTS = {
    "analyst": (
        "你是一位在买方机构（公募私募）从业 8 年的 A 股盘面分析师。"
        "你的分析框架：首先确认量价关系（是否有量能支撑），然后判断资金属性（主力/散户/北向），最后看技术指标位置。"
        "你倾向于右侧交易思维，不做左侧预测。"
        "回答风格：先用一句话定性盘面，再分点展开推理过程。"
    ),

    "morning_brief": (
        "你是一位拥有 15 年以上经验的 A 股首席策略分析师，曾任职于头部券商研究所。"
        "你的分析框架：隔夜外盘 → 消息面过滤 → A 股联动逻辑 → 技术位参考（支撑/压力）→ 持仓应对。"
        "特别擅长从外盘变化推导 A 股的传导路径和程度。"
        "回答风格：先给今日核心判断（1-2 句），再按结构展开。判断须标注置信度。"
    ),

    "midday_review": (
        "你是一位专注盘中交易的资深策略分析师，擅长在上午盘面信号中捕捉下午的变盘线索。"
        "你的分析框架：上午量价特征（放量/缩量/震荡/突破）→ 热点持续性判断 → 北向资金态度 → 下午走势推演。"
        "特别注意识别\"诱多\"和\"假摔\"信号。"
        "回答风格：先定性上午走势类型，再给出下午两种可能情景及概率。"
    ),

    "evening_review": (
        "你是一位以绝对收益为目标的资深投资经理，管理过 10 亿以上资金，"
        "擅长从持仓管理和风险控制角度做日度复盘。"
        "你的分析框架：当日盈亏归因（β 还是 α）→ 每只持仓的资金行为分析 → 技术状态评估 → 次日多情景预案。"
        "对每只持仓必须给出明确的处理逻辑，不给出模糊的\"继续持有\"建议。"
        "回答风格：辛辣直接，每一个操作建议都必须同时给出触发条件和失效条件。"
    ),

    "fund_expert": (
        "你是一位拥有 15 年经验的基金研究专家，曾任晨星（Morningstar）高级分析师。"
        "你熟悉中国公募基金行业的所有明星基金经理。"
        "你的分析框架：定量指标（夏普/回撤/Alpha）→ 持仓风格穿透 → 规模变动分析 → 经理能力圈判断。"
        "特别关注基金的\"风格漂移\"风险——即基金实际持仓是否偏离了其招募说明书宣称的风格。"
        "回答风格：客观、数据驱动，对每只基金的判断须标注\"数据支持度\"（强/中/弱）。"
    ),
}
```

### 主要变化

| 维度 | 原来 | 现在 |
|------|------|------|
| 身份锚点 | 模糊 | 有具体机构类型和年限 |
| 分析框架 | 无 | 每位角色有固定分析流程 |
| 风格约束 | 无 | 统一为右侧/绝对收益导向 |
| 回答约束 | 只有字数 | 有推理过程显式化的要求 |
| 置信度机制 | 无 | 关键判断标注高/中/低 |

---

## 二、实时盯盘分析 Prompt 优化（ai_analyzer.py）

### 当前问题

1. 200 字限制与 3 方面分析要求矛盾
2. 没有推理链引导
3. 缺少成交量/涨跌家数比等关键数据
4. 技术指标只是陈列没有交叉分析要求

### 修改方案

#### 2.1 增加数据维度

```python
# 在 _build_prompt 函数中，在 "## 市场状态" 部分增加：

# 成交量分析（从 quotes 中统计）
total_volume = sum(q.volume for q in quotes if q.volume is not None)
advance_volume = sum(q.volume for q in quotes if q.volume and q.change_pct and q.change_pct > 0)
decline_volume = sum(q.volume for q in quotes if q.volume and q.change_pct and q.change_pct < 0)

lines.append(f"- 监控标的涨跌家数: {stats.up}涨 / {stats.down}跌 / {stats.flat}平")
if total_volume > 0:
    lines.append(f"- 上涨家数占比: {stats.up/(stats.up+stats.down+stats.flat)*100:.0f}%")
    lines.append(f"- 上涨量比: {advance_volume/total_volume*100:.0f}%（上涨标的成交量/总成交量）")
```

#### 2.2 重写分析要求（取消 200 字限制）

原代码第 101-109 行替换为：

```python
lines.extend([
    "",
    "请按以下框架分析，输出控制在 300 字以内：",
    "",
    "### 第一步：盘面定性（1 句）",
    "用一个词或短语定性当前盘面（如：缩量震荡、放量上攻、冲高回落等），然后一句话说明核心矛盾。",
    "",
    "### 第二步：量价验证",
    "- 当前上涨是否有量能支撑？上涨量比是否大于 50%？",
    "- 如果缩量上涨，需标注\"缩量上涨，持续性存疑\"",
    "- 如果放量下跌，需标注\"抛压较重，谨慎\"",
    "",
    "### 第三步：异动解读",
    "对警报标的逐一说明：是技术性回调、资金驱动还是基本面因素？",
    "",
    "### 第四步：关键技术位判断",
    "结合 RSI（超买/超卖）、MACD（金叉/死叉）、KDJ 的位置，给出交叉验证结论：",
    "- 多个指标是否指向同一方向？（如 RSI 超买 + KDJ 高位死叉 = 短期回调压力大）",
    "- 指标是否出现背离？（如价格新高但 RSI 未创新高 = 顶背离风险）",
    "",
    "### 置信度标注",
    "对以上判断标注置信度：[高] 数据充分且指标一致 / [中] 数据尚可但指标有分歧 / [低] 数据不足或方向不明",
    "",
    "注意: 如果数据不足以得出明确结论，请明确说\"数据不足\"，不要强行分析。",
])
```

#### 2.3 调整调用参数

```python
result = llm.chat(
    prompt=prompt,
    system_prompt=SYSTEM_PROMPTS["analyst"],
    max_tokens=800,    # 500 → 800
    temperature=0.3,
)
```

---

## 三、早盘简报 Prompt 优化（reporter.py - generate_morning_brief）

### 当前问题

1. 隔夜市场缺少 A50/汇率/美债收益率关键数据
2. 6 点分析要求是并列关系，没有主次和推理链
3. 持仓技术诊断和应对策略写在一起但缺少"条件-行动"映射

### 3.1 数据输入增强

在 `generate_morning_brief` 函数中，获取 A50 期指和汇率数据（新增函数）：

```python
def _fetch_a50_and_fx() -> dict:
    """获取 A50 期指和人民币汇率作为开盘参考"""
    result = {}
    try:
        # 富时中国 A50 期货（新加坡交易所）
        resp = eastmoney_client.get(
            "https://push2.eastmoney.com/api/qt/stock/get",
            params={"secid": "1.0000XSC", "fields": "f2,f3,f4"}
        )
        if resp:
            data = resp.json().get("data", {})
            result["A50期指"] = f"{data.get('f3', '--'):+.2f}%"

        # 美元/人民币离岸汇率
        resp2 = eastmoney_client.get(
            "https://push2.eastmoney.com/api/qt/stock/get",
            params={"secid": "1.USDCNH", "fields": "f2,f3"}
        )
        if resp2:
            data2 = resp2.json().get("data", {})
            result["离岸人民币"] = f"{data2.get('f2', '--')}"
    except Exception:
        pass
    return result
```

在构建 LLM prompt 的数据段中追加这些数据：

```python
# A50 & FX
a50_fx = _fetch_a50_and_fx()
if a50_fx:
    extra_market_parts = [f"{k}: {v}" for k, v in a50_fx.items()]
    llm_lines.append(f"  {' | '.join(extra_market_parts)}")
```

### 3.2 分析要求重写

当前第 342-353 行的 `llm_lines.append(f"""...""")` 替换为：

```python
llm_lines.append("""

请按以下结构生成早盘简报（约 600 字，关键判断标注置信度）：

### 一、隔夜传导判断（置信度：[高/中/低]）
- 隔夜美股/A50/港股的综合影响是正面、中性还是负面？
- 推导逻辑：外盘涨跌 → 对应的 A 股传导链条是什么？

### 二、今日走势推演
给出两种情景及概率：
- **基准情景（60%+）**：认为最可能的走势，给出波动区间参考
- **风险情景（20-30%）**：如果什么超预期事件发生会导致走势偏离预期
- **关键观察点**：开盘后前 30 分钟的量价特征

### 三、持仓应对预案（表格形式）
| 持仓 | 技术状态 | 若高开 | 若平开 | 若低开 | 止损位 |
|------|---------|--------|--------|--------|--------|
| ... | ... | ... | ... | ... | ... |

### 四、风险预警
今日最需要关注的 1-2 个风险点。

要求：每一个判断都必须标注置信度。如果数据不足以支撑判断，如实说"数据不足"。
""")
```

---

## 四、午评 Prompt 优化（reporter.py - generate_midday_review）

### 当前问题

1. 上午盘面特征没有要求 LLM 与昨日同时段做对比（同比）
2. "下午走势预测"过于笼统，没有要求具体到时间节点和触发条件
3. PE/市值/换手率的数据说明放在 prompt 中属于"污染"——它应该是系统知识而非每次发送

### 4.1 分析要求重写

当前第 536-554 行替换为：

```python
llm_lines.append("""

请按以下结构分析（约 500 字）：

### 一、上午盘面定性
- 用一句话定性上午走势（如：缩量震荡、放量上攻、冲高回落、V 型反转等）
- 与昨日同时段对比：量是放大还是缩小了？（放量/缩量/持平）

### 二、热点与资金
- 领涨/领跌品种的特征：大盘还是小盘？价值还是成长？
- 涨停/跌停家数反映的市场情绪强度
- 北向资金的态度与方向

### 三、下午走势推演（置信度标注）
- **情景一（大概率）**：下午可能的走势路径
- **情景二（小概率但需警惕）**：与预期相反的信号出现时的应对
- **关键观察窗口**：下午 1:30 和 2:30 两个节点需要关注什么

### 四、持仓应对（条件格式）
对每只持仓输出：
- 目前盈亏状态
- 下午若翻红/翻绿到什么程度需要行动（给出具体的盈亏点位）
- 止损/止盈触发条件

要求：判断须标注置信度。操作建议必须是"如果 X 则 Y"的条件格式，不能是模糊的"继续观察"。
""")
```

---

## 五、晚报 Prompt 优化（reporter.py - generate_evening_review）

### 当前问题

1. 7 个分析点太多，LLM 容易每个点都蜻蜓点水
2. 没有要求归因分析（今天的盈亏多少来自 β、多少来自 α）
3. "明日操作清单"缺少情景分类

### 5.1 分析要求重写

当前第 807-830 行替换为：

```python
llm_lines.append("""

请按以下结构生成晚报（约 700 字）：

### 一、持仓全景归因
- 今日总盈亏：{total_pnl:+,.2f}
- 归因分析：多少来自市场整体涨跌（β），多少来自持仓自身表现（α）？
- 强弱分化：哪只最强/最弱，差距原因是什么？

### 二、重点持仓深度点评（按重要性排序，不超过 3 只）
对每只持仓输出：
- **技术状态**：价在均线什么位置？RSI/MACD/KDJ 是否同向？
- **资金行为**：主力主导还是散户主导？有无异常放量/缩量？
- **估值水平**：结合 PE/PB 判断贵贱（标注置信度）
- **核心判断**：一句话结论（如："短期超买，明日有回调需求"）

### 三、明日多情景预案

| 情景 | 触发条件 | 应对动作 | 置信度 |
|------|---------|---------|--------|
| 乐观 | 大盘高开+放量 | ... | 中 |
| 基准 | 平开震荡 | ... | 高 |
| 悲观 | 低开+北向流出 | ... | 中 |

### 四、风控红线
明日每只持仓的硬止损位和硬止盈位（具体价格或盈亏百分比）。

要求：必须使用条件格式（if-then），标注置信度。不输出与持仓无关的市场分析。
""")
```

### 5.2 增加 β/α 归因计算

在 `generate_evening_review` 中，增加一个简单的归因函数：

```python
def _simple_attribution(holdings_results: list[dict], market_return: float) -> dict:
    """简单的持仓归因：将持仓收益拆解为 β（市场）和 α（选股）部分
    
    Args:
        holdings_results: _holdings_summary 的返回结果
        market_return: 大盘当日涨跌幅（如沪深300）
        
    Returns:
        {"beta_pnl": float, "alpha_pnl": float, "beta_pct": str, "alpha_pct": str}
    """
    total_pnl = sum(h["pnl"] for h in holdings_results)
    total_cost = sum(h["cost"] * h["amount"] for h in holdings_results if hasattr(h, "cost"))
    
    # β 收益 = 总成本 × 大盘涨幅（假设组合 β=1）
    beta_pnl = total_cost * market_return
    alpha_pnl = total_pnl - beta_pnl
    
    return {
        "beta_pnl": round(beta_pnl, 2),
        "alpha_pnl": round(alpha_pnl, 2),
        "total_pnl": round(total_pnl, 2),
    }
```

---

## 六、基金分析 Prompt 优化（fund_analyzer.py）

### 当前问题

1. 缺少前十大持仓数据，导致风格匹配度判断缺乏依据
2. 没有要求分析"规模变动 vs 业绩"的关系
3. 没有"风格漂移"检测机制
4. 总字数 2000 字在基金数量较多时不够

### 6.1 数据输入增强

在 `analyze_funds` 中，增加一段从查询中提取的持仓穿透信息到 prompt（当 mx-data 不可用时，也可以从净值和基准数据反推）：

```python
# 在构建 prompt 时，增加风格分析数据段
style_analysis = []
for f in funds_with_metrics:
    bm = f.get("benchmark_metrics", {})
    beta = bm.get("beta", "--")
    alpha = bm.get("alpha", "--")
    
    # 从 Beta 反推风格
    if beta != "--" and beta != 0:
        if beta > 1.2:
            style = "高弹性/激进型（Beta>1.2，大盘涨时涨更多，跌时跌更深）"
        elif beta > 0.8:
            style = "市场同步型（Beta 0.8-1.2，与大盘基本同步）"
        else:
            style = "稳健型（Beta<0.8，波动小于大盘）"
    else:
        style = "数据不足，无法判断"
    
    # 从 Alpha 判断超额收益能力
    if alpha != "--":
        if alpha > 5:
            alpha_judge = "显著正 Alpha，基金经理创造稳定超额收益"
        elif alpha > 0:
            alpha_judge = "微弱正 Alpha，超额收益不明显"
        elif alpha > -5:
            alpha_judge = "微弱负 Alpha，略跑输基准"
        else:
            alpha_judge = "显著负 Alpha，持续跑输基准，需警惕"
    else:
        alpha_judge = "数据不足"
    
    style_analysis.append(
        f"- {f['name']}({f['code']}): {style} | {alpha_judge}"
    )

# 将 style_analysis 插入 prompt 中
```

### 6.2 分析要求重写

在 prompt 的 `### 2. 报告输出要求` 部分（第 1007-1040 行），对深度研判部分做以下修改：

原 `#### **🔍 深度研判与经理点评**` 部分替换为：

```
#### **🔍 深度研判与风格分析**

##### a. 风格定位
基于 Beta 系数和持仓特征，将基金归入以下三类之一：
- **进攻型**（Beta > 1.2）：适合牛市/反弹行情
- **均衡型**（Beta 0.8 - 1.2）：适合震荡市
- **防御型**（Beta < 0.8）：适合熊市/避险行情

##### b. 超额收益质量评估
- 区分"运气"和"能力"：Alpha 是否稳定？还是靠某几只股票的偶然爆发？
- 与基准的跟踪误差是否在合理范围内？

##### c. 风格漂移检测
对比基金的历史风格标签与最新 Beta 值：
- 如果之前是防御型最近 Beta 突然变大，说明经理可能在做风格轮动
- 如果风格与招募说明书不一致，标注"⚠️ 存在风格漂移风险"

##### d. 规模-收益匹配度
- 基金规模 vs 历史表现：规模增长后收益是否下滑？（规模是业绩的敌人）
- 如果规模 > 50 亿但依然高换手，标注"大规模高换手，需关注冲击成本"

##### e. 操作建议
每只基金给出具体建议，格式为"条件 → 行动"：
- "如果市场风格切换到成长，该基金是优先加仓标的"
- "如果该基金规模继续扩张至 100 亿以上，需重新评估其选股能力"
```

### 6.3 增加字数上限

`_call_llm` 的 `max_tokens` 从 2500 增加到 3000-3500，以适应更多分析内容。同时 system prompt 中的字数限制也相应调整。

```python
return llm.chat(
    prompt=prompt,
    system_prompt=SYSTEM_PROMPTS["fund_expert"],
    max_tokens=3500,
    temperature=0.3,
)
```

---

## 七、配置与温度策略

### 当前参数

所有 LLM 调用使用统一的 temperature=0.3。

### 优化参数矩阵

| 场景 | Temperature | Max Tokens | 角色 | 说明 |
|------|-----------|-----------|------|------|
| 实时盯盘分析 | 0.3 | 800 | analyst | 确定性判断，temperature 保持低值 |
| 早盘简报 | 0.4 | 2000 | morning_brief | 需要适度发散，考虑多种外盘传导路径 |
| 午评 | 0.3 | 1500 | midday_review | 盘中判断，确定性优先 |
| 晚报 | 0.3 | 2000 | evening_review | 复盘总结，确定性优先 |
| 基金分析 | 0.3 | 3500 | fund_expert | 数据驱动的定量+定性分析 |

---

## 八、通用优化建议（跨场景）

### 8.1 所有 prompt 统一增加的约束

在每个 user prompt 末尾追加：

```python
lines.append("")
lines.append("---")
lines.append("**置信度标注规则**：对于每一个判断性结论，请在括号中标注你的确定程度。")
lines.append("- [高]：数据充分、指标一致、历史模式明确")
lines.append("- [中]：数据尚可但存在分歧信号")
lines.append("- [低]：数据不足或逻辑链条不完整")
lines.append("**不确定性处理**：如果数据不足以支持判断，请直接输出\"数据不足\"而非强行给出结论。")
```

### 8.2 成本优化

- Morning Brief 和 Evening Review 的 max_tokens 可以从 2000 调整到 1500-1800，因为 prompt 要求更精准、冗余更少
- 温度差异化为 0.4 的 Morning Brief 略微增加了生成多样性成本，但换来更多情景覆盖，整体 ROI 为正

---

## 九、实施优先级与影响评估

| 优先级 | 改动项 | 影响范围 | 预计效果 | 风险 |
|--------|--------|---------|---------|------|
| P0 | System prompt 重写 | 所有场景 | 中-高 | 低（只改文本） |
| P0 | 增加置信度标注要求 | 所有场景 | 高 | 低（只追加文本） |
| P0 | 增加成交量/涨跌家数数据 | 实时分析 | 高 | 低（已有数据源） |
| P1 | 分析框架从"大纲"改为"推理链" | 所有报告 | 高 | 中（输出格式变化） |
| P1 | 持仓应对改为"条件-行动"格式 | 三个报告 | 高 | 中（输出格式变化） |
| P1 | 温度参数差异化 | 所有场景 | 中 | 低 |
| P2 | 增加 A50/汇率/美债收益数据 | Morning Brief | 中 | 中（新增数据源） |
| P2 | 增加多情景分析 | Morning Brief/Evening | 中 | 中（输出变长） |
| P2 | 基金分析增加持仓穿透 | 基金分析 | 高 | 高（数据获取依赖外部） |
| P2 | β/α 归因计算 | Evening Review | 中 | 低（纯计算逻辑） |
